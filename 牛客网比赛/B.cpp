#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
#define ll long long
ll n, q, x, l, r, maxx, lastans, minx, tot, pos1, posm, las1;
int main() {
	scanf("%lld%lld", &n, &q);
	while (q--) {
		scanf("%lld", &x);
		l = 1, r = n;
		lower_bound( , )
	}
}