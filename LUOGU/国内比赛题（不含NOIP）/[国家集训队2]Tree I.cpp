#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
#define MAXN 100010
struct Edge {
	int u, v, c;
}e[MAXN];
bool Kruskal(int k) {

}
int n, m, k;
int main() {
	scanf("%d%d%d", &n, &m, &k);
	for (int i = 1; i <= m; i++) {
		scanf("%d%d%d%d", &e[i].u, &e[i].v, &e[i].w, &e[i].c);
		
	}
}