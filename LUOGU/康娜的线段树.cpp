#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
#define MAXN 1001100
int a[MAXN << 2], n, m, x, y, k;
int main() {
	scanf("%d%d", &n, &m);
	for (int i = 1; i <= n; i++) {
		scanf("%d", &a[i]);
	}
}